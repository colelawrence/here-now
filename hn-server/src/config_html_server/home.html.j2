<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Here Now Configuration</title>
  <link rel="shortcut icon" href="duckyhn.png" type="image/png">
  {# Tailwind utilities and base stuff #}
  <link rel="stylesheet" href="config-html-server.css">
  {# Our download of htmx #}
  <script src="unpkg.com_htmx.org@1.9.2_dist_htmx.min.js"></script>
</head>

<body class="p-16">
  <h1 class="text-title-2xl font-bold">Here Now Configuration</h1>
  <style>
    .dev-links a {
      display: flex;
      gap: 0.4em;
      align-items: center;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
    }

    .dev-links a img {
      height: 1em;
    }

    .dev-links a:hover {
      background: rgba(128, 128, 128, 0.2);
    }
  </style>
  <p class="flex gap-2 dev-links">
    <a href="/dev/docs/hn_server/index.html" target="_blank">📦 Cargo Docs</a>
    <a href="/dev/traces/search?service=hn-server" target="_blank"><img src="/dev/traces/static/jaeger-logo-ab11f618.svg"> Traces</a>
    <a href="/data" target="_blank">💽 Data Browser</a>
    <a href="http://0.0.0.0:9000" target="_blank"><img src="http://0.0.0.0:9000/public/favicon.png"> Public</a>
  </p>
  <p>You're now looking at the self-configuration page, where we'll set up your service.</p>
  {% for conf in confs %}
  <h2 class="pt-8 text-title-xl font-bold">{{ conf.section_name }}</h2>
  {{ conf.view_html }}
  {% endfor %}
</body>

</html>