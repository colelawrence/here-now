[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

[features]
default = []
docs = ["clap/unstable-doc"]

[dependencies]
clap = { version = "4.3.18", features = ["derive"] }
jod-thread = "0.1.2"
